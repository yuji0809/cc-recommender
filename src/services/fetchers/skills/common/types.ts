/**
 * Common types for skill fetching
 */

export type RepoMetadata = {
  name: string;
  description: string;
  stars: number;
  url: string;
  owner: string;
  ownerUrl: string;
  topics: string[];
};
